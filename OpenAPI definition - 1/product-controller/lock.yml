info:
  name: lock
  type: http
  seq: 5
  tags:
    - product-controller

http:
  method: POST
  url: http://localhost:8999/v1/products/lock
  body:
    type: json
    data: |-
      {
        "items": [
          {
            "id": "",
            "quantity": 0
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/v1/products/lock"
      method: POST
      body:
        type: json
        data: |-
          {
            "items": [
              {
                "id": "",
                "quantity": 0
              }
            ]
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "data": false,
            "message": ""
          }
