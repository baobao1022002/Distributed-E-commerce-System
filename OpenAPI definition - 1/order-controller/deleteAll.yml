info:
  name: deleteAll
  type: http
  seq: 3
  tags:
    - order-controller

http:
  method: DELETE
  url: http://localhost:8181/v1/orders
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/v1/orders"
      method: DELETE
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "data": "",
            "message": ""
          }
