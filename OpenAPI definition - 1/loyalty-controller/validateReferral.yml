info:
  name: validateReferral
  type: http
  seq: 6
  tags:
    - loyalty-controller

http:
  method: GET
  url: http://localhost:8185/api/loyalty/referrals/:code/validate
  params:
    - name: code
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/loyalty/referrals/:code/validate"
      method: GET
      params:
        - name: code
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "{}"
