info:
  name: generateReferral
  type: http
  seq: 2
  tags:
    - loyalty-controller

http:
  method: POST
  url: http://localhost:8185/api/loyalty/referrals/generate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/loyalty/referrals/generate"
      method: POST
      params:
        - name: userId
          value: ""
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: "{}"
