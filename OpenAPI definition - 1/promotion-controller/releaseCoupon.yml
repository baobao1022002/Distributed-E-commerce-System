info:
  name: releaseCoupon
  type: http
  seq: 4
  tags:
    - promotion-controller

http:
  method: POST
  url: http://localhost:8083/api/promotions/coupons/:code/release
  params:
    - name: code
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/promotions/coupons/:code/release"
      method: POST
      params:
        - name: code
          value: ""
          type: path
        - name: orderId
          value: ""
          type: query
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
