info:
  name: participateFlashSale
  type: http
  seq: 2
  tags:
    - promotion-controller

http:
  method: POST
  url: http://localhost:8083/api/promotions/flash-sales/:id/participate
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/promotions/flash-sales/:id/participate"
      method: POST
      params:
        - name: id
          value: ""
          type: path
        - name: quantity
          value: "1"
          type: query
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: ""
